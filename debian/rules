#!/usr/bin/make -f
# -*- makefile -*-

#export DH_VERBOSE=1
#export PYBUILD_NAME=classicmenu_indicator
export PYBUILD_DESTDIR=debian/classicmenu-indicator

%:
	dh $@ --with python3 --buildsystem=pybuild
